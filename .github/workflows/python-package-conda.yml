

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5


      with:
        python-version: '3.10'
    - name: 
      run: |
      
    - name: Install dependencies
      run: |

    - name: Lint with flake8
      run: |
s
    - name: Test with pytest
      run: |

